import React from 'react';
import LogoIcon from "../../icons/LogoIcon";
import {Typography} from "@material-ui/core";

const Logo = () => {
    return (
        <div>
            <LogoIcon />
            <Typography>

            </Typography>
        </div>
    );
};

export default Logo;